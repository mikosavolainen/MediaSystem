import React from 'react';
import MediaUpload from '../components/MediaUpload';

const UploadMedia = () => (
    <div>
        <h2>Upload Media</h2>
        <MediaUpload />
    </div>
);

export default UploadMedia;
